services:
  droid-apikey:
    image: ghcr.io/dofgm/droid-apikey:latest
    container_name: droid-apikey
    ports:
      - "4693:4693"
    environment:
      - EXPORT_PASSWORD=admin
    restart: unless-stopped
